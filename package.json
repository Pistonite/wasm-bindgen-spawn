{
  "scripts": {
    "build:web": "node build.web.mjs",
    "build:no-modules": "node build.no-modules.mjs",
    "build": "npm-run-all --parallel build:web build:no-modules"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "esbuild": "^0.28.1",
    "npm-run-all": "^4.1.5"
  }
}
